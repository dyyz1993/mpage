---
globs:
  - 'src/**/*'
  - 'bin/**/*'
  - 'tests/**/*'
---

# mpage 代码约束

- 禁止 import xcli 的任何模块（`xcli/` 是独立项目）
- 公共 API 通过 `src/index.ts` 统一导出，新增导出必须在此文件注册
- 单文件不超过 300 行，超过必须拆分为独立模块
- 新代码不允许新增 `any` 类型，用具体类型或 `unknown` + 类型守卫替代
