---
globs:
  - '.xcli/plugins/*/index.ts'
keywords:
  - tips
  - ok
  - return
match: any
---

# 插件 Tips 输出质量标准

## tips 必须包含实质信息

### 好 tips（包含数量 + 关键值）
- `"采集到 5 篇文章"`
- `"采集到 15 个链接，含 3 个外链"`
- `"房产数据: 5 个小区，均价 72000-92000 元/平"`
- `"股票: 平安银行(000001) 当前价 ¥12.55，涨 +0.80%"`
- `"验证码类型: slider，目标位置: x=180"`

### 差 tips（只有模糊描述）
- `"数据已采集"`
- `"房产数据已采集"`
- `"验证码检测完成"`

## 格式规范
- 第 1 条 tips 必须是数据摘要（数量 + 关键指标）
- 第 2+ 条 tips 可以是补充信息（发现的问题、特殊处理等）
- 中文逗号分隔多个数值
- 价格/百分比保留原始格式

## 在 ok() 调用中构造 tips
```typescript
return ok(data, [
  `采集到 ${data.length} 条数据`,
  `关键字段: ${data.slice(0, 3).map(d => d.title).join(', ')}`,
]);
```
