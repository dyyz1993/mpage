name: Publish

on:
  push:
    branches: [master]
    paths:
      - package.json
      - packages/core/package.json
      - packages/create-xcli/package.json
  workflow_dispatch:
    inputs:
      dry_run:
        description: Dry run (skip actual publish)
        required: false
        default: false
        type: boolean

jobs:
  publish:
    runs-on: ubuntu-latest
    needs: []
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 10

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          registry-url: https://registry.npmjs.org/

      - run: pnpm install --no-frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Build all
        run: pnpm run build

      - name: Test
        run: npx vitest run

      - name: Publish @dyyz1993/xpage
        run: npm publish ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build packages
        run: pnpm run build:packages

      - name: Publish @dyyz1993/xcli-core
        working-directory: packages/core
        run: npm publish ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish create-xcli
        working-directory: packages/create-xcli
        run: npm publish ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
